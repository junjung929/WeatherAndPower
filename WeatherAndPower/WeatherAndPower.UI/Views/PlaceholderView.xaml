<UserControl x:Class="WeatherAndPower.UI.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WeatherAndPower.UI"
			 d:DataContext="{d:DesignInstance local:PlaceholderViewModel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="100">
	<UserControl.Resources>
		<ResourceDictionary Source="/WeatherAndPower.UI;component/ValueConverters/ValueConverter.xaml"/>
	</UserControl.Resources>
	<Grid Margin="0">
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<Button Command="{ Binding PlaceholderCommand }" Content="TestButton" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5" Width="75"/>
		<Label Content="{ Binding PlaceholderProperty, Mode=OneWay }" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5"/>
	</Grid>
</UserControl>
